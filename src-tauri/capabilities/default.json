{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main Codex Switcher window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
